# OCR Watcher

Watches for images in the clipboard and performs OCR on them (using Tesseract).

# Development

### Must have Nix (with flakes enabled), direnv, and nix-direnv installed before running this

```bash
direnv allow
uv sync
```

# Running

```bash
python -m ocr_watcher
```
